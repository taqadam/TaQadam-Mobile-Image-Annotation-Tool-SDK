<?xml version="1.0" encoding="utf-8"?>
<layout>
    <data>

        <variable
            name="Assignment"
            type="com.recoded.taqadam.models.Assignment" />
    </data>

    <android.support.v7.widget.CardView
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        xmlns:tools="http://schemas.android.com/tools"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_margin="8dp"
        app:cardElevation="4dp"
        app:cardUseCompatPadding="true"
        app:contentPadding="8dp">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:orientation="vertical">

            <TextView
                android:id="@+id/job_name"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_margin="2dp"
                android:text="@{Assignment.job.title}"
                android:textSize="22sp"
                android:textStyle="bold"
                tools:text="Label No. of cats"/>

            <TextView
                android:id="@+id/job_desc"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginLeft="8dp"
                android:layout_marginRight="8dp"
                android:layout_marginBottom="4dp"
                android:text="@{Assignment.job.description}"
                android:textSize="16sp"
                tools:text="Label No. of catsLabel No. of catsLabel No. of catsLabel No. of cats"/>

            <View
                android:layout_width="match_parent"
                android:layout_height="1dp"
                android:background="@color/colorPrimary"/>

            <RelativeLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_margin="4dp">

                <TextView
                    android:id="@+id/job_type"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_alignParentLeft="true"
                    android:layout_alignParentStart="true"
                    android:background="@color/colorPrimary"
                    android:padding="8dp"
                    android:text="@{Assignment.type.name.toUpperCase()}"
                    android:textColor="@color/colorWhite"
                    tools:text="@string/task_type"/>

                <TextView
                    android:id="@+id/images_count"
                    android:visibility="invisible"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_centerHorizontal="true"
                    android:background="@color/colorPrimary"
                    android:padding="8dp"
                    android:text="@{String.format(@string/job_item_tasks, String.valueOf(Assignment.job.dataset.size))}"
                    android:textColor="@color/colorWhite"
                    tools:text="8000 Tasks"/>

                <TextView
                    android:id="@+id/job_reward"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_alignParentEnd="true"
                    android:layout_alignParentRight="true"
                    android:background="@color/colorPrimary"
                    android:padding="8dp"
                    android:text="@{Assignment.job.service.name}"
                    android:textColor="@color/colorWhite"
                    tools:text="$0.5"/>
            </RelativeLayout>
        </LinearLayout>
    </android.support.v7.widget.CardView>
</layout>